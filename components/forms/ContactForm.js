import { ModalCloseButton, ModalContent, ModalHeader, ModalBody } from "@chakra-ui/react";

const ContactForm = () => {
  return (
    <ModalContent>
      <ModalHeader>Kontakt oss</ModalHeader>
      <ModalCloseButton />
      <ModalBody></ModalBody>
    </ModalContent>
  );
};

export default ContactForm;
